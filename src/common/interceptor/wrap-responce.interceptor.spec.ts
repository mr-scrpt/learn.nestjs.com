import { WrapResponceInterceptor } from './wrap-responce.interceptor';

describe('WrapResponceInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapResponceInterceptor()).toBeDefined();
  });
});
